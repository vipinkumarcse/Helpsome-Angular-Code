import { FormatphonenewPipe } from './formatphonenew.pipe';

describe('FormatphonenewPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatphonenewPipe();
    expect(pipe).toBeTruthy();
  });
});
